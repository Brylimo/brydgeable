extends base
include mixins/items

block content 
	div.profile__data
		img(src="/" + user.avatarUrl).avatar
		h3=user.name
	div.video-grid
		each item in user.items
			+items(item)
		else 
			li Sorry nothing found.