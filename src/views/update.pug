extends base

block content 
	form(method="POST")
		input(name="title" type="text" value=item.title)
		input(name="description" type="text" value=item.description)
		input(name="hashtags" type="text" value=item.hashtags.toString())
		input(type="submit" value="update")